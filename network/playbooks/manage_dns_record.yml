---
- name: Manage a DNS record on request
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Run domain role
      ansible.builtin.include_role:
        name: manage_dns_record

    - name: Write result back to ServiceNow
      ansible.builtin.include_role:
        name: servicenow_writeback
