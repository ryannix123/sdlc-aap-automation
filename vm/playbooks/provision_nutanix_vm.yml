---
- name: Provision a Nutanix VM on request
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Run domain role
      ansible.builtin.include_role:
        name: provision_nutanix_vm

    - name: Write result back to ServiceNow
      ansible.builtin.include_role:
        name: servicenow_writeback
